<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.accounting">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_monetization_on_96_purpledark"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pieChart.PieChartActivity"></activity>
        <activity android:name=".SplashActivity"></activity>
        <activity android:name=".pieChart.PieChartFragment" />
        <activity
            android:name=".main.MainDrawerActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".itemInfo.ItemInfoActivity" />
        <activity android:name=".addNewItem.AddNewActivity" />
        <activity android:name=".main.MainPagerFragment" />
    </application>

</manifest>